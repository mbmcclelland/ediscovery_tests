# eDiscovery Test Suite — Python dependencies
# Install: pip install -r requirements.txt
# Or for the dr-load CLI (editable install): pip install -e .

# HTTP client
requests>=2.31.0

# Functional test framework
pytest>=7.4.0
pytest-html>=4.1.0
pytest-xdist>=3.5.0

# Load testing
locust>=2.20.0

# CLI / TUI frameworks
typer>=0.9.0
rich>=13.0.0
textual>=0.40.0

# Config / environment
python-dotenv>=1.0.0
pydantic>=2.5.0

# Database (Postgres, used by JobPoller in dr-load)
psycopg2-binary>=2.8.0

# Browser automation (used by playwright_fresh_install.py and
# playwright_fresh_init.py for the post-install DR setup workflow).
# After install, run `playwright install chromium` once to fetch the
# browser binary.
playwright>=1.40.0

# Traffic-capture proxy (proxy_logger.py addon). Used to record
# Digital Reef REST traffic into /tmp/dr_proxy_capture.json while
# the Playwright scripts drive the UI — captures the request bodies
# we use to build endpoint references (docs/endpoints_v0.0[56].md).
mitmproxy>=10.0.0

# Local-host metrics for the dr-tui landing dashboard
# (CPU / memory / network / disk IOPS / top processes).
psutil>=5.9.0
